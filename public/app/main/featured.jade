.panel.panel-primary
    .panel-heading Featured Courses
    .panel-body
        .row(ng-repeat="course in courses | filter:{featured : true}")
            .col-md-12
                {{course.name}}
        